- Add "copy code" button to code blocks (accessibility feature)

    - Should we avoid copying `>>>` in REPL sessions?

- Add a list of all questions so that it's not super annoying to navigate

    - Is it possible to use `<details>` both on desktop and mobile?

- Add some testing of examples (maybe with pyodide)?

- Syntax highlighting (how do we do this witn minimal amount of bloat...)
